import { View, styled, XStack, YStack } from "tamagui";

export const Container = styled(View, {
    flex: 1,
    backgroundColor: "$color1",
    alignItems: "center",
    justifyContent: "center",
    position: "relative"
});

export const ContainerX = styled(XStack, {
    flex: 1,
    backgroundColor: "$color1",
    alignItems: "center",
    justifyContent: "center",
    position: "relative"
});

export const ContainerY = styled(YStack, {
    flex: 1,
    backgroundColor: "$color1",
    alignItems: "center",
    justifyContent: "center",
    position: "relative"
})